{
  "name": "producer",
  "private": true,
  "version": "0.1.0",
  "description": "Faceless + avatar YouTube automation. BYO-keys, tenant-ready. Vercel dashboard, Render worker, Replicate GPU.",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "db:push": "pnpm --filter @producer/core db:push",
    "db:studio": "pnpm --filter @producer/core db:studio",
    "worker": "pnpm --filter @producer/worker start",
    "worker:dev": "pnpm --filter @producer/worker dev",
    "web:dev": "pnpm --filter @producer/web dev"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
